apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookshop.fullname" . }}-test-connection"
  labels:
{{ include "bookshop.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bookshop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
